name: Source Gap Audit

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: source-gap-audit-${{ github.ref }}
  cancel-in-progress: true

jobs:
  source-gap-audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Configure Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Self-test scanner
        run: python3 scripts/audit_source_gaps.py --self-test

      - name: Scan compiled source
        run: python3 scripts/audit_source_gaps.py --no-receipt
