<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration
    
    Enforces secure network communication practices:
    - HTTPS/TLS enforcement
    - Certificate pinning for critical services
    - No cleartext traffic allowed
    
    Standards Compliance:
    - OWASP Mobile Top 10 (M3: Insecure Communication)
    - NIST SP 800-52 (TLS Configuration)
    - ISO/IEC 27001 (A.13: Communications Security)
-->
<network-security-config>
    <!-- Base configuration for all connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificate authorities -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- GitHub API Configuration -->
    <!-- Note: Certificate pinning is disabled for flexibility. 
         In production, enable pinning by uncommenting the pin-set section
         and updating with actual certificate pins obtained from:
         openssl s_client -connect api.github.com:443 | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | base64
    -->
    <domain-config>
        <domain includeSubdomains="true">github.com</domain>
        <domain includeSubdomains="true">api.github.com</domain>
        <!-- Certificate pinning disabled - uncomment to enable:
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">PRIMARY_PIN_HERE</pin>
            <pin digest="SHA-256">BACKUP_PIN_HERE</pin>
        </pin-set>
        -->
    </domain-config>
    
    <!-- GitLab Configuration -->
    <!-- Note: Certificate pinning is disabled for flexibility.
         In production, enable pinning and update with actual certificate pins.
    -->
    <domain-config>
        <domain includeSubdomains="true">gitlab.com</domain>
        <!-- Certificate pinning disabled - uncomment to enable:
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">PRIMARY_PIN_HERE</pin>
            <pin digest="SHA-256">BACKUP_PIN_HERE</pin>
        </pin-set>
        -->
    </domain-config>
    
    <!-- Development Configuration (Debug builds only) -->
    <!-- This configuration is overridden in debug builds to allow local testing -->
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user-added certificates for debugging -->
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
