# Neurocognitive/Biophysical Federated Gate V1

Status: `DRAFT_CONTROL_PLANE`; `claim_allowed=false`.

## Preflight
Resolve identity → authority → endpoint → evidence state → falsifier → gate → destination → receipt.

Missing material field becomes `TOKEN_VAZIO`; it is never guessed.

## Gate flow

`Mapa state -> Papers claim/limits -> RafPolimata static/reproducible checks -> external experiment where required -> receipt -> Mapa delta/index`

RafGitTools routes and audits this movement. It does not manufacture scientific validity.

## High-risk boundaries

1. DNA sequence vs damage/adduct vs methylation/expression.
2. intention vs molecular editing mechanism.
3. ionizing radiation vs non-ionizing EMF.
4. physical EM field vs social-field metaphor.
5. UPE measurement vs brain-source attribution vs neural communication.
6. internal reproduction vs independent replication.

## External-only gates

The following cannot be closed by repository CI or documentation:
- intervention/epigenetic mediation replication;
- diet exposure molecular endpoint study;
- controlled EMF dosimetry biological study;
- channel perturbation/block/rescue biological experiment;
- cross-scale language/social mediation study when making biological causal claims;
- brain-source-attributed UPE replication.

## Receipt semantics

`PASS_STATIC_CONTRACT` proves only that a record obeys the contract.
`REPRODUCED_INTERNAL` proves only a declared internal reproducibility scope.
`REPLICATED_INDEPENDENT` requires genuinely separate authority/environment/method disclosure.
`PASS` remains claim-specific and never propagates to neighboring claims.

## R3
F_ok: authority-aware gate contract materialized.
F_gap: remote CI and external biological receipts remain absent until executed.
F_next: attach PR/Drive IDs to a federated receipt and ingest future evidence without automatic promotion.
